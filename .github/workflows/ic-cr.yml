name: Build updated mkdocs container
on:
  - push

jobs:
  build:
    name: Build image
    runs-on: ubuntu-latest
    env:
      IMAGE_NAME: cde-mkdocs-ce
      REGISTRY: us.icr.io
    steps:

    - name: Clone the repository
      uses: actions/checkout@v2

    - name: Buildah Action
      id: build-image
      uses: redhat-actions/buildah-build@v2
      with:
        image: ${{ env.IMAGE_NAME }}
        tags: latest ${{ github.sha }}
        containerfiles: |
          ./Dockerfile

    - name: Log in to the IBM Cloud Global Container registry
      uses: redhat-actions/podman-login@v1
      with:
        registry: ${{ env.REGISTRY }}
        username: iamapikey
        password: ${{ secrets.REGISTRY_PASSWORD }}

    - name: Push to IBM Cloud Global Container Repository
      id: push-to-icr
      uses: redhat-actions/push-to-registry@v2
      with:
        image: ${{ steps.build-image.outputs.image }}
        tags: ${{ steps.build-image.outputs.tags }}
        registry: ${{ env.REGISTRY }}



# name: Log in to IBM Container Registry 
# on:
#   push:

# env:
#   REGISTRY_USER: iamapikey
#   IMAGE_REGISTRY: 
#   REGISTRY_PASSWORD: ${{ secrets.REGISTRY_PASSWORD }}

# jobs:
#   login:
#     name: Log in to image registry
#     runs-on: ubuntu-20.04
#     steps:

#       - name: Log in to US IBM Cloud Registry endpoint 
#         uses: redhat-actions/podman-login@v1
#         with:
#           username: ${{ env.REGISTRY_USER }}
#           password: ${{ env.REGISTRY_PASSWORD }}
#           registry: ${{ env.IMAGE_REGISTRY }}

  
#       - name: Push To quay.io
#         id: push-to-quay
#         uses: redhat-actions/push-to-registry@v2
#         with:
#           image: ${{ steps.build-image.outputs.image }}
#           tags: ${{ steps.build-image.outputs.tags }}


#     - name: Print image url
#       run: echo "Image pushed to ${{ steps.push-to-quay.outputs.registry-paths }}"